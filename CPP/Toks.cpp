#include<stdio.h>

class Toks :: class StreamTokenizer{
   static String IF = "if";
   static String AND = "and";
   static String DOT = ".";
   static String HOLDS = "holds";
   static String LISTS = "lists"; // todo
   static String IS = "is"; 
  
  static Toks makeToks(String s, boolean fromFile) {
  
    try{
      fopen R;
    
  }
};
